import React from 'react'
import {Container} from './style'

// import { connect } from "react-redux";

export default function Recommends({id}) {
    return (
        <Container>
            Recommends
        </Container>
    )
}
